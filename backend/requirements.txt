fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
pydantic==2.8.2
email-validator==2.2.0
groq==0.9.0
resend==2.2.0
bleach==6.1.0
httpx==0.27.0

# NOTE: slowapi removed — we use a simple built-in rate limiter instead
# to avoid the slowapi/limits version conflict that crashes on Render
