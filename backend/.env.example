# ── Groq (FREE AI chat — no credit card needed) ───────────────────────────────
# Sign up at console.groq.com → API Keys → Create key
# Free tier: 14,400 requests/day — way more than enough for a portfolio
GROQ_API_KEY=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

# ── Resend (FREE email — no credit card needed) ───────────────────────────────
# Sign up at resend.com → API Keys → Create key
# Free tier: 3,000 emails/month — plenty for a contact form
RESEND_API_KEY=re_To4prDae_MRZ36JHBUigUzhkJCKSD8K2k

# ── Where contact form emails get delivered ────────────────────────────────────
CONTACT_EMAIL_TO=alhasanal_qaysi@yahoo.com

# ── CORS: which frontend URLs are allowed to call this backend ─────────────────
# Local dev: http://localhost:3000
# After Vercel deploy: https://your-app.vercel.app
# Comma-separated if you have both
ALLOWED_ORIGINS=http://localhost:3000

# ── App mode ───────────────────────────────────────────────────────────────────
# Set to "production" on Render — hides the /docs page
ENVIRONMENT=production
